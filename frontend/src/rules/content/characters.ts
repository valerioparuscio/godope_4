import type { RulePage } from '../types';

// Stub — backfilled after the v1 shell ships (see the plan's "Contenuto
// iniziale" ordering). Pages to add here: characters/criminal,
// characters/link.
export const CHARACTERS_PAGES: RulePage[] = [];
